{
  "name": "pinterest",
  "version": "1.0.0",
  "description": "<span style=\"font-family: helvetica\">This is Pinterest-type website. \r Image API to display random photos and make some other features.</span>\r <p>Unfortunately, the API version, which I use allows getting only 30 photos per request</p>",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
